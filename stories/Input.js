import React from 'react';

export default props => (
  <input
    {...props}
    style={{
      padding: '10px',
      fontSize: '14px',
      width: '100%',
      boxSizing: 'border-box',
    }}
  />
);
